import { createSlice } from "@reduxjs/toolkit";

export const businessPayload = createSlice({
    initialState: { reference: '', recipient: "", username: "", mode: 'email' },
    name: "businessPayload",
    reducers: {
        
    }
})